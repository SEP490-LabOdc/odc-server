spring:
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: false
      username: admin
      password: admin123
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1